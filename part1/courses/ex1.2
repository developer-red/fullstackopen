const Header = ({course}) => {

    return (<div>
        <h1> {course}</h1>
    </div>)
}

const Content = function ({props}) {
    return (
        < div>
            <Part part={props.part1} exercises={props.exercises1}/>
            <Part part={props.part2} exercises={props.exercises2}/>
            <Part part={props.part3} exercises={props.exercises3}/>
        </div>
    )
}

const Part = function ({part, exercises}) {
    return (
        <p> {part} {exercises} </p>
    )
}

const Total = function ({props}) {
    console.log(props)

    console.log(typeof props.exercises1, props.exercises1);
    console.log(typeof props.exercises2, props.exercises2);
    console.log(typeof props.exercises3, props.exercises3);

    const total = props.exercises1 + props.exercises2 + props.exercises3
    console.log(total)
    return (<div>
            <p>Number of exercises {total}</p>
        </div>
    )
}

const App = () => {
    const course = 'Half Stack application development'
    const part1 = 'Fundamentals of React'
    const exercises1 = 10
    const part2 = 'Using props to pass data'
    const exercises2 = 7
    const part3 = 'State of a component'
    const exercises3 = 14

    return (
        <div>
            <Header  course={course}/>
            <Content props={{part1, part2, part3, exercises1, exercises2, exercises3}}/>
            <Total   props={{exercises1, exercises2, exercises3}}/>
        </div>
    )
}
export default App
